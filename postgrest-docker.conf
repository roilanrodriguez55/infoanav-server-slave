# URI de conexión — el host "db" solo resuelve dentro de docker compose (mismo proyecto/red).
# Fuera de esa red, usa postgrest.conf (localhost) o define PGRST_DB_URI en el entorno (prioridad sobre esta línea).
# Formato: postgres://usuario:contraseña@host:puerto/nombre_db
db-uri = "postgres://postgres:admin@db:5432/infoanav"

# El esquema que PostgREST expondrá
db-schemas = "api, auth"

# Extra schemas for name resolution (must include public if triggers/RPCs call pgcrypto, etc.)
db-extra-search-path = "api, auth, public"

# El rol que se usará para peticiones sin token JWT
db-anon-role = "anon"

# El secreto JWT para validar los tokens
# Debe tener al menos 32 caracteres para ser seguro
jwt-secret = "12345678123456781234567812345678"

# Opcional: Puerto en el que correrá el servidor de PostgREST
server-port = 3000
